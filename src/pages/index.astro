---
import Header from '../Components/Header.astro';
import Footer from '../Components/Footer.astro';
import HeroSection from '../Components/HeroSection.astro';
import DestinationCard from '../Components/DestinationCard.astro';
import OfferCard from '../Components/OfferCard.astro';
import Newsletter from '../Components/Newsletter.astro';
import '../styles/home.css';
import { destinations, offers } from '../script/data';// Asegúrate de tener estos datos o ajusta según tus necesidades
---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<title>Astro</title>
	</head>
	<body>
		
      <Header />
      
      <HeroSection
        title="¡Descubre el Mundo con Nosotros!"
        subtitle="Las mejores ofertas y destinos para tus próximas vacaciones."
        ctaText="Explorar Destinos"
        ctaLink="/destinos"
      />
      
      <section class="destinos">
        <h2>Destinos Destacados</h2>
        <div class="destinos-grid">
          {destinations.map(destination => (
            <DestinationCard
              key={destination.id}
              image={destination.image}
              title={destination.title}
              description={destination.description}
              link={`/destinos/${destination.id}`}
            />
          ))}
        </div>
      </section>
      
      <section class="ofertas">
        <h2>Ofertas Especiales</h2>
        <div class="ofertas-grid">
          {offers.map(offer => (
            <OfferCard
              key={offer.id}
              image={offer.image}
              title={offer.title}
              price={offer.price}
              link={`/ofertas/${offer.id}`}
            />
          ))}
        </div>
      </section>
      
      <section class="servicios">
        <h2>Nuestros Servicios</h2>
        <p>Ofrecemos una amplia gama de servicios para hacer que tu experiencia de viaje sea inolvidable, desde vuelos y hoteles hasta transporte y excursiones.</p>
      </section>
      
      <section class="testimonios">
        <h2>Lo Que Dicen Nuestros Clientes</h2>
        <div class="testimonios-list">
          <!-- Aquí puedes agregar testimonios reales -->
          <blockquote>
            <p>"¡La mejor agencia de viajes! Todo estuvo perfectamente organizado y nos hicieron sentir como en casa."</p>
            <footer>- Cliente Satisfecho</footer>
          </blockquote>
          <blockquote>
            <p>"Recomiendo encarecidamente sus servicios. ¡Las ofertas son inigualables y el servicio al cliente excepcional!"</p>
            <footer>- Otro Cliente Feliz</footer>
          </blockquote>
        </div>
      </section>
      
      <Newsletter />
      
      <Footer />
    
	</body>
</html>


